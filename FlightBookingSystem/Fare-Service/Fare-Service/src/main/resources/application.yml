server:
 port : 9192
spring:
  data: 
    mongodb:
       uri : mongodb+srv://dbadmin:dbadmin@cluster0.rbazw.mongodb.net/FlightFare?retryWrites=true&w=majority

  application: 
    name: Fare-Service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
       defaultZone: http://localhost:8761/eureka
  instance:
     hostname: localhost

 